import { createStore } from 'vuex'
import {formDataModule} from "@/store/formDataModule";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    postForm: formDataModule
  }
})
